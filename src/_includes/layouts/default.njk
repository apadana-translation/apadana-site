<!DOCTYPE html>
<html lang="en">

  {% include 'head.njk' %}

  <body>

    {% include 'analytics--noscript.njk' %}
    {% include 'svg-sprite.njk' %}

    <div class="content">
      {% include 'site__header.njk' %}
      <main role="main">
        <div class="container">
          {{ content | safe }}
        </div>
      </main>
      {% include 'site__footer.njk' %}
    </div>

    {# % asset main.js async % #}

    <script>
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", user => {
          if (!user) {
            window.netlifyIdentity.on("login", () => {
              document.location.href = "/admin/";
            });
          }
        });
      }
    </script>

  </body>
</html>
