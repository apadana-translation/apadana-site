% Set main font to Skolar PE from src/_assets/fonts (path is relative to the
% project root, where config/pandoc/job.mjs runs pandoc)
\setmainfont [
  Path = src/_assets/fonts/,
  Extension = .ttf,
  UprightFont = *-Regular,
  BoldFont = *-Bold,
  ItalicFont = *-Italic,
  BoldItalicFont = *-BoldItalic
] {SkolarPEWeb}

% Set page header
% adapted from https://github.com/edsl/endefensadelsl.org/blob/queue/src/layouts/header.tex
\usepackage{setspace}
\usepackage{fancyhdr}

\pagestyle{fancy}

\setlength{\headheight}{16pt}
\fancyhf{}
\fancyhead[LE]{\footnotesize{\nouppercase{\textsc{\thepage \quad Legends of the Buddhist Saints}}}}
\fancyhead[RO]{\footnotesize{\nouppercase{\textsc{\leftmark \quad \thepage}}}}

% \renewcommand*{\chaptermark}[1]{\markboth{#1}{}}

\raggedbottom
